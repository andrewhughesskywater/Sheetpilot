/**
 * Compatibility re-export for WebformFiller.
 *
 * The implementation lives under `src/browser/webform_flow.ts`.
 *
 * Keep this file to preserve older import paths. Prefer importing the browser
 * implementation from `src/browser/` when you work inside this package.
 */
export * from './browser/webform_flow';

