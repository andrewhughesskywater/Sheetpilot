/**
 * Compatibility re-export for WebformFiller.
 *
 * The implementation lives under `src/browser/webform_flow.ts`, but consumers
 * historically imported from `src/webform_flow`.
 */
export * from './browser/webform_flow';

