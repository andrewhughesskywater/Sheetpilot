/**
 * Public entrypoint for the bot package.
 *
 * The main implementation lives under `src/core/index.ts`.
 *
 * Keep this file small: it exists to preserve older import paths like
 * `.../services/bot/src` after the internal folder refactor.
 */
export * from './core/index';

