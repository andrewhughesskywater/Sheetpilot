/**
 * Public entrypoint for the bot package.
 *
 * The main implementation lives under `src/core/index.ts`. This file exists so
 * imports like `.../bot/src/index` keep working after the folder refactor.
 */
export * from './core/index';

